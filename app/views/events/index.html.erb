<h1><%= t("events.event.all") %></h1>

<div class="mb-3">
  <%= link_to t("events.event.create_event"), new_event_path, class: 'btn btn-success' %>
</div>

<div class="row mb-3">
  <%= render partial: 'events/event', collection: @events %>
</div>
