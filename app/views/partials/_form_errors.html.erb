<% errors = model.errors %>

<% if errors.any? %>
  <div class="alert alert-danger mt-3">
    <h4 class="alert-heading">
      <%= pluralize(errors.count, "error") %> prohibited this user from being saved:
    </h4>
    <ul>
      <% errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
